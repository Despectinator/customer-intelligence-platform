export default function ActivityTable({ activities = [] }) {
  return <section className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold text-slate-900">Recent Activity</h2>{activities.length === 0 ? <p className="mt-5 rounded-xl border border-dashed border-gray-300 px-4 py-10 text-center text-sm text-slate-500">Recent activity will appear here.</p> : <div className="mt-5 overflow-x-auto"><table className="w-full text-left text-sm"><tbody className="divide-y divide-gray-200 text-slate-600">{activities.map((activity) => <tr key={activity.id}><td className="py-3">{activity.label}</td><td className="py-3">{activity.date}</td><td className="py-3">{activity.status}</td></tr>)}</tbody></table></div>}</section>;
}
