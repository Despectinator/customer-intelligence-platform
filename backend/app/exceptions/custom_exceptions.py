"""
Application-specific exception classes (e.g. ProjectNotFoundError,
InvalidCSVError). Raised from the services layer, translated to HTTP
responses by handlers.py.

To be implemented as each service module needs them.
"""
