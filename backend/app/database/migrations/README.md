# Migrations

Not in use yet — the schema is currently managed directly via
`database/schema.sql`, run manually in the Supabase SQL editor.

If the schema needs versioned migrations later (e.g. via Alembic), they
belong in this folder.
